-=-= German:
Das fertige Programm ist für den Raspberry bestimmt. DH. ist eine echte "Windows-Version" sinnfrei. 
Da ich selbst unter Windows mit haupsaechlich mit CodeBlocks arbeite, so finden sich auch Windows-Optionen im Source.

Fuer den Raspberry

gcc -s -O2 -Wall fake-hwclock.c -o fake-hwclock

fake-hwclock liegt im Verzeichnis /sbin. Vorher das alte Script umbennen. Dann reinkopieren.

cp fake-hwclock /sbin

Das wars dann auch schon.

-=-= English:
The finished program is intended for the Raspberry. IE. is a real "Windows version" meaningless.
Since I myself mainly work with CodeBlocks under Windows, there are also Windows options in the source.

For the Raspberry

gcc -s -O2 -Wall fake-hwclock.c -o fake-hwclock

fake-hwclock is in the /sbin directory. Rename the old script first. Then copy it.

cp fake-hwclock /sbin

That's it.
